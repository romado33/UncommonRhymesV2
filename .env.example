# Provider (choose one)
UR_LLM_PROVIDER=openai
# UR_LLM_PROVIDER=hf

# OpenAI (if using openai)
# OPENAI_API_KEY=sk-...

# Hugging Face Inference (if using hf)
# HUGGINGFACEHUB_API_TOKEN=hf_...

# Features (0 = OFF, 1 = ON) — keep OFF for now
UR_LLM_RERANK=0
UR_LLM_PATTERN_RERANK=0
UR_LLM_OOV_G2P=0
UR_LLM_PHRASE_GEN=0
UR_LLM_MULTIWORD_MINEDOG=0
UR_LLM_NL_QUERY=0

# Runtime knobs
UR_LLM_TIMEOUT_S=8.0
UR_LLM_MAX_TOKENS=256