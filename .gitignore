# Python
__pycache__/
*.pyc
.env

# Virtual env
.venv/

# OS junk
.DS_Store
Thumbs.db

# Local data (keep CMU + full DB local)
data/cmudict-0.7b.txt
data/patterns.db

# Optional: keep these out unless you *want* them in GitHub
# (OK if each <100 MB; remove these lines if you plan to deploy to HF Spaces straight from GitHub)
data/words_index.sqlite
data/patterns_small.db
results/
*.db.bak
